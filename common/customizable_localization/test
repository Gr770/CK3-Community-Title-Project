DukeType = {
	type = character
	text = {
		trigger = {
			has_culture = anglo_saxon
			vassal_contract_has_flag = has_march_contract # Does't seem to detect this
		}
		localization_key = marquess_anglo_saxon
	}
	text = {
		localization_key = duke_contract_default
		fallback = yes
	}
}
